def fibonacci(n):
    sequence = [0, 1]
    while len(sequence) < n:
        sequence.append(sequence[-1] + sequence[-2])
    return sequence

terms = 10
fib_seq = fibonacci(terms)
print(f"Fibonacci sequence of {terms} terms:", fib_seq)
